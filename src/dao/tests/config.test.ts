

const mockInvalidClient = {
    user: 'ss',
    host: 'd',
    database: 'aa',
    password: 'rem',
    port: 1111,
    };

describe('createConnection', () => {
    // it('should connect to pg', () => {

    // });
});